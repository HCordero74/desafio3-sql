Creación de base de datos.
Creación de Tablas.
Ingreso de datos a las tablas.
Consultas a múltiples tablas utilizando inner join, left join, sub tablas, where, group by, having, order by, limit y MAX.
